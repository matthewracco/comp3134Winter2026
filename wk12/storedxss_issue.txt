Stored XSS occurs when the user input or data which is stored is displayed without any sanitization. In this example, the app is reading the content from a txt file and outputs it directly to the browser. Because there is no sanitization, the script tag is being executed in the browser. This is dangerous because malicious scripts can be executed this way, and every user that accesses the page will execute this script.